<%= form_for @attraction do |form| %>
	<%= form.label :name %>
	<%= form.text_field :name %>
	<%= form.label :travelguides_id %>
	<%= form.number_field :travelguides_id %>
	<%= form.submit "Submit" %>

<% end %>