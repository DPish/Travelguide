<%= form_for @attraction do |form| %>
	<%= form.label :name %>
	<%= form.text_field :name %>
	<%= form.label :travelguide_id %>
	<%= form.text_field :travelguide_id %>
	<%= form.submit "Submit" %>

<% end %>